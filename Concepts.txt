to amend the previous commit we use the amend with the next commit
 git add .
 git commit -m "version 1" --amend.
 git reset file name or .  to take files out of staging area.  git checkout -- filename or .

 git pull is used to pull the updated code to the local repo if there is any change in github repo.
 git fetch , git pull origin master
 